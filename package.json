{
	"name": "cline-proxy",
	"version": "0.1.0",
	"description": "Local OpenAI-compatible server that routes requests through your Cline CLI provider configuration.",
	"license": "Apache-2.0",
	"type": "module",
	"bin": {
		"cline-proxy": "./dist/cli.js"
	},
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"test": "bun test",
		"serve": "bun src/cli.ts serve"
	},
	"dependencies": {
		"@cline/core": "0.0.83",
		"@cline/llms": "0.0.83",
		"@cline/shared": "0.0.83"
	},
	"devDependencies": {
		"@types/node": "^22.9.0",
		"bun-types": "^1.4.2",
		"typescript": "^5.6.3"
	},
	"engines": {
		"node": ">=22"
	}
}
